const initialState = {
  loading: true,
  items: [],
  error: null,
};

export const medications = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
